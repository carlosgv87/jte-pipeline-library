def debug(message) {
    echo "[DEBUG] ${message}"
}

def error(message) {
    echo "[ERROR] ${message}"
}

def info(message) {
    echo "[INFO] ${message}"
}

def warn(message) {
    echo "[WARNING] ${message}"
}